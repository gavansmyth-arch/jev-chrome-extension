{
  "name": "jev-chrome-extension",
  "version": "2.0.5",
  "private": true,
  "description": "Jev for Chrome: drive the tab you're looking at with TypeSafe Jev, or ask it about any text.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bg/*.js shared/*.js panel/*.js options/*.js content/*.js && npm test"
  },
  "author": "Gavan Smyth",
  "license": "MIT"
}
